import React from 'react';

export const LargeSizeAds = (props) => {
    return (
        <React.Fragment>
            <img src={props.img} className="large-ads-img"/>
        </React.Fragment>
    )
}
